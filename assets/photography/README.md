# Held photography

Final photographs that are approved but not currently placed on any page.

Nothing here is served: files under `public/` are deployed verbatim, so an
unreferenced photo there is dead weight on every deploy. Move a file into
`public/placeholders/` when a page actually uses it.

| File | Depicts |
| --- | --- |
| `bedroom-cream.webp` | BackEasy Sheets made up on a cream upholstered bed. Held for a future slot. |
