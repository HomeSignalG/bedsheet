# Placeholder image assets

Every file in this directory is a clearly labeled placeholder awaiting final
photography. To replace one, drop in the final image and either:

1. Keep the same filename (converting to `.jpg`/`.png`/`.webp` and updating
   the `src` path where the file is referenced), or
2. Update the `src` prop where the image is used.

All images are rendered through `components/PlaceholderImage.tsx`, so sizing
and lazy-loading behavior stays consistent when files are swapped.

| File | Used on | Depicts (final photo) |
| --- | --- | --- |
| `home-hero.svg` | Home hero | Removable top being lifted/detached from the bed |
| `lifestyle-ease.svg` | Home lifestyle | Easier physical process of changing the top |
| `lifestyle-kids.svg` | Home lifestyle | A child (~age 10) / teen changing their own bed |
| `lifestyle-household.svg` | Home lifestyle | Real-life households, families, pets |
| `product-hero.svg` | Product hero | Clean isolated product visual |
| `construction-attachment.svg` | Product | Attachment mechanism close-up |
| `construction-top.svg` | Product | Removable top construction |
| `construction-base.svg` | Product | Fitted base |
| `construction-corner.svg` | Product | Corner / retention construction |
| `construction-seams.svg` | Product | Seams / stitching |
| `construction-fabric.svg` | Product | Fabric close-up |
